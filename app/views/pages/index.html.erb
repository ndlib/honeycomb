<% page_title(t('.title'), @collection) %>
<%= collection_nav(@collection, :pages) %>

<%= render partial: 'action_bar', locals: { collection: @collection } %>

<%= react_component 'EntriesList', {
    header: t(".title"),
    entryType: "page",
    entries: @list_items,
    openUrl: "/pages/<id>/edit",
  }
%>
